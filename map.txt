make_token-encrypt(token) >> decrypt(token)-make_alias-encrypt(alias) >> decrypt(alias)-login(jsession) >> download_scans(jsession) >> organize

download exam:
    get mrsessions
        if _makedirs
            fix perms


    download bids
        download
            fix perms
        unzip
            fix perms

    download gre 
        download
            fix_perms
        unzip
            fix perms

    organize
        fix perms